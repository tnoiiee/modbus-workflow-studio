/**
 * Application version label rendered in the UI.
 *
 * Kept in one place so the UI label, root/client/server package versions, the
 * startup banner, and `GET /api/health` stay synchronized (acceptance J06).
 */
export const APP_VERSION = '1.2.12';
