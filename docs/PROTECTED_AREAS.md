# Protected Areas

React Flow, dynamic ports, connections, Undo/Redo, workflow CRUD, reliable auto-save, concurrent runtime isolation, shared Modbus queues, Modbus write safety, Audit Viewer, Modbus Monitor, logic symbols, status lamps, and Linear Mapping must not regress without approved scope.
