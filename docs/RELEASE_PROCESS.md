# Release Process

Approve scope, verify base, synchronize versions, validate with `npm run check`, create and verify a clean ZIP, merge through review, tag, release, and update current state and changelog.
