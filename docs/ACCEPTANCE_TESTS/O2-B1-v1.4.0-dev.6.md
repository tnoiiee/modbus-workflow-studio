# O2-B1 v1.4.0-dev.6 — Stage 1 and Full Gates evidence


## Current Owner acceptance / development checkpoint delivery

Owner INTERMEDIATE ACCEPTANCE accepts the Stage 1 implementation and counts below and authorizes
Full Gates, final scope verification, exactly one commit and normal push to
`arena/01a0d291-modbus-workflow-studio`. No implementation scope expansion is authorized.
**Owner Local Manual Review remains PENDING.** No PR, Tag, Release, ZIP or later-phase work.

The fresh sandbox again had stale HEAD `eed588481ae7e7376f9926e58dbd37f9076b5c2e`.
Actual Remote matched the required base `20b929bb5bdd82673173764efab1effc98c2aa5a`.
Fresh external backup/patch/untracked list and SHA-256 evidence covered all 211 source files.
One targeted fetch supplied the base object. Archive comparison found exactly the approved
14 modified + 17 new O2-B1 files, 180 unchanged base files, no missing/unexpected source.
CAS pointer recovery and `read-tree` without `-u` preserved all 211 implementation hashes.
Temporary evidence is current-environment only, NOT a durable backup.

Dependencies were absent and restored exactly once with
`npm ci --include=optional --ignore-scripts` (249 packages). All preserved source hashes remained
unchanged afterward. Manifests/lock differ from dev.5 only in approved application version metadata;
dependency versions/resolution/integrity are unchanged. Dependencies remain ignored/untracked.
The restoration reported 5 moderate, 1 high, 1 critical advisories: NOT resolved, NOT accepted,
NOT part of O2-B1. No audit fix or upgrade.

Before Full Gates, protected infrastructure diff inspection verified unchanged zero-based packet
encoding, output ownership/guards, Commanded/Effective/Read-back separation, Monitor cancellation,
existing relative write/Workflow/Monitor priority, and authoritative manual disconnect. New acquisition
work remains lower-priority and read-only. MBAP length/bounds, context/transaction/generation checks,
fragment/coalesced processing and disconnect buffer clearing remain the accepted Stage 1 implementation.
An exact inverse-diff check of `index.ts` reconstructs the base after removing only approved service
composition, configuration route registration, lifecycle wiring and version changes. Existing WS,
Workflow/Monitor behavior, production-write and Definition/Overview contracts have no other edits.

### Full Gates — current run

| Sequential gate | Result |
| --- | --- |
| `npm test -w client -- --run` | PASS — 523 tests / 36 files |
| `npm test -w server -- --run` | PASS — 247 tests / 16 files |
| `npm run check` | PASS — both typechecks, Server 247/16, Client 523/36, both builds |
| `node scripts/hygiene-check.mjs --all --strict` | PASS — 411 files, 0 errors, 0 warnings |
| `npm run verify:publish` | PASS — 411 files, 0 errors, 0 warnings |
| `git diff --check` | PASS |

Standalone Stage 1 selectors were NOT rerun. The normal `npm run check` test invocations are
part of its approved contract. No Full Gate test/build failure or source correction occurred.
The first Client shell wrapper returned an anomalous tool status after explicitly printing
`FULL_CLIENT_EXIT=0`; Vitest reported 523/523 PASS and the command was not repeated. Later
`npm run check` independently ran the same full Client suite successfully under its normal contract.

Warnings preserved: SSR `useLayoutEffect` and JS bundle **645.10 kB** (187.71 kB gzip; >500 kB).
These automated results are not hardware, real-browser manual or prolonged-soak acceptance.
All six Full Gates passed in the authorized sequence without a gate retry or code correction.
Only documentation was updated to record Owner acceptance, results and pending manual review.
Commit/push verification is recorded in the Owner delivery handoff after the authorized gates;
a commit cannot embed its own final SHA. This is a development checkpoint, not final acceptance.

### Owner Local Manual Review — all PENDING

| Review item | Status |
| --- | --- |
| Acquisition mapping UI and validation | PENDING |
| Manual disconnect remains authoritative | PENDING |
| No auto-connect behavior | PENDING |
| No Modbus writes from O2-B1 | PENDING |
| Existing Workflow writes remain unchanged | PENDING |
| Existing Modbus Monitor remains unchanged | PENDING |
| Workflow and Monitor regression | PENDING |
| Device reconnect behavior | PENDING |
| Runtime state not persisted into Page or Definition data | PENDING |
| Tag Runtime invisible to Overview in dev.6 | PENDING |
| Owner-local hardware simulator or Device validation | PENDING |
| Extended operation or soak validation | PENDING |

Known limitations remain: Shared-Tag-only compatible identical-range dedup, cross-owner duplicates,
no general String codec, no WORKFLOW_VARIABLE producer, no REST snapshot/Tag WS/live Overview,
no hardware validation or prolonged-soak certification, not hard real-time; higher-priority queue
pressure may make Tags STALE. No MQTT/Sparkplug, controls, cross-owner broker or later phases.
O2-B2/O2-B3/O2-C/O2-D: NOT STARTED.

## Historical accepted Stage 1 evidence

The sections below record the preceding Stage 1 turn, not the current delivery authorization.

## Authority / checkpoint

Owner FINAL APPROVAL authorizes O2-B1 implementation and Stage 1 only.
Base: `20b929bb5bdd82673173764efab1effc98c2aa5a` / v1.4.0-dev.5.
Branch: `arena/01a0d291-modbus-workflow-studio`.
O2-A: Owner APPROVED / Local Manual Review PASS.
Target: v1.4.0-dev.6. Intermediate Owner review pending; not final acceptance.

Before editing, actual `git ls-remote` matched the approved base. The recreated sandbox had stale
local HEAD `eed588481ae7e7376f9926e58dbd37f9076b5c2e`. Backed up all 194 source files, diff and
untracked list externally in the current environment, fetched the approved branch once, compared
all source paths/bytes against its archive (different/missing/extra all zero), then used exact-old-SHA
CAS `update-ref` and `read-tree` without `-u`. All 194 hashes remained unchanged; clean/staged 0.
These temporary files are NOT durable backups. The source was not reconstructed or overwritten.
Dependencies were absent; restored from the approved lock with
`npm ci --ignore-scripts --no-audit --no-fund` (249 packages). No upgrades/audit fixes.

## Final Stage 1 results

| Gate | Tests / files | Result |
| --- | ---: | --- |
| TCP framing | 36 / 1 | PASS |
| Acquisition configuration + HTTP CRUD | 50 / 1 | PASS |
| Scheduler / Runtime Store / real server lifecycle | 47 / 3 | PASS |
| Targeted protected server regression | 114 / 11 | PASS |
| Targeted client configuration/Overview/Definition/version regression | 372 / 18 | PASS |
| Client typecheck | — | PASS |
| Server typecheck | — | PASS |
| Server build | — | PASS |
| Client build | — | PASS |

Unique selected tests: server **247 / 16 files**, client **372 / 18 files**.
These are staged targeted selections, NOT authorization or evidence for Full Gates.
Client output: 645.10 kB JS (187.71 kB gzip); existing >500 kB bundle warning remains.
No dependency audit was run. Known dev.5 advisories remain 5 moderate / 1 high / 1 critical,
Not resolved / Not accepted / outside this checkpoint.

### Initial failures and corrections (not hidden by final results)

1. First framing command: 26 passed / 10 failed of 36 in one file. A new test used the unavailable
   Vitest 2 matcher `toHaveBeenCalledExactlyOnceWith`. Replaced it with separate count/argument
   assertions; no production code change for this failure. Rerun 36/36 PASS.
2. First client selection: 339 passed / 1 failed of 340 in 16 files. An existing hard-coded dev.5
   assertion in `overviewState.test.ts` had not yet been synchronized; updated its version-only
   assertions to dev.6. The initial selector also named nonexistent `DataSourcesReferences.test.tsx`
   (Vitest ignored it); corrected to actual `DataSourcesUX.test.tsx` and `ReferenceDetails.test.tsx`.
   Final expanded selection 372/372 in 18 files PASS.
3. Added four wire-order Float64/FC04 cases and an IEEE NaN acquisition case after the first
   scheduler run (42/3 PASS); final selection is 47/3. Final Stage 1 rerun covers the final source.

No test assertions were removed to weaken production acceptance; no dependency upgrades were used.

## Exact selected commands

From repository root, in Stage 1 order:

```sh
npm test -w server -- --run test/modbusFraming.test.ts
npm test -w server -- --run test/acquisitionConfig.test.ts
npm test -w server -- --run test/sharedTagAcquisition.test.ts test/tagRuntime.test.ts test/acquisitionLifecycle.test.ts
npm test -w server -- --run test/acquisitionRegression.test.ts test/core.test.ts test/reliability.test.ts test/liveTransport.test.ts test/definitionCatalog.test.ts test/definitionRoutes.test.ts test/definitionBatchReferences.test.ts test/definitionReferences.test.ts test/definitionBindingIntegration.test.ts test/overviewPages.test.ts test/overviewControlStates.test.ts
npm test -w client -- --run src/components/sources/AcquisitionMappingEditor.test.tsx src/components/sources/DataSourcesPage.test.tsx src/components/sources/DataSourcesUX.test.tsx src/components/sources/ReferenceDetails.test.tsx src/components/overview/OverviewSelectionUX.test.tsx src/components/overview/OverviewGridUX.test.tsx src/components/overview/InspectorTransition.test.tsx src/components/overview/InspectorUX.test.tsx src/lib/overviewProtectedO2.test.ts src/lib/overviewBinding.test.ts src/lib/overviewBindingO2.test.ts src/lib/overviewElements.test.ts src/lib/overviewState.test.ts src/lib/overviewNavigation.test.ts src/lib/overviewFontDraft.test.ts src/lib/definitionReferences.test.ts src/lib/definitionDeleteApi.test.ts src/version.test.ts
npm run typecheck -w client
npm run typecheck -w server
npm run build -w server
npm run build -w client
```

## Coverage / evidence boundaries

- Deterministic parser tests: header/body fragments, coalesced frames, MBAP protocol/length,
  oversized frames and 260-byte buffer. Connection tests: timeouts/late frames, context before
  request, reconnect/old generation, mismatched responses, exceptions, cancellation, queue bounds
  and write/Workflow/Monitor priority, FC05/06/16 frame construction/zero-based addresses.
- Configuration: supported FC01–04 and all numeric widths, finite scaling, strict fields and
  bounds, existing Shared Tag/Device, rejection of variable identity and String/Hex, enable/disable,
  persistence/reload/orphans, limits, actual configuration-only HTTP CRUD and no snapshot route.
- Scheduler: browser/workflow-independent ownership, one outstanding scan, identical-read dedup,
  separate scaling, group/global limits, manual disconnect, stale success AND error across mapping
  and connection changes, disable/delete/type/device invalidation, finite decoding, Boolean false,
  all four Float64 byte/word orders, FC04, NaN, malformed payload, last-good and STALE transitions.
- Runtime: startup/no sample, five qualities, 0/false, last-good/receive time, source time null,
  monotonic freshness, epoch/sequence/generation, remove/reactivate, byte/count/text/observer bounds,
  defensive copies and observer failure isolation. No persistent runtime/replay file.
- A real child server, TCP simulator and WS clients verify: startup/WS reconnect never connects a
  Device; explicit existing Device connect starts reads with all Workflows stopped; reads outlive
  browsers; disconnect stops reads and WS reconnect cannot restart them; no Tag WS delivery or
  snapshot endpoint; health/hello versions match dev.6. This is NOT a real browser or hardware test.
- Protected regression: concurrent Workflow reads/session isolation, stop/disconnect, LIVE_ARMED
  guard, write ownership/write-on-change/commanded-effective separation; independent Monitor;
  existing queue/WS reliability and Definition/binding/reference/Overview persistence tests.
- Actual persisted Definition/mapping/Page bytes and a cloned Draft are unchanged by acquisition;
  valid WORKFLOW_VARIABLE binding remains BOUND without a producer.
- Client configuration tests are API/helper/SSR/source-contract tests. They do NOT establish real
  browser pointer/focus/layout, accessibility or hardware acceptance.

## Owner local/manual review checklist — NOT RUN here

- Configure supported Number and Boolean mappings using a simulator; verify field errors, disabled
  defaults, save/reload, load failure/retry, remove confirmation and focus recovery.
- Device must already be connected; manual disconnect must remain authoritative across browser
  close/reopen. No startup auto-connect. Confirm reads continue with no browser or running Workflow.
- Verify Definition editing/Delete/reference details and Overview Editor Preview are unchanged;
  no Page revision, Draft/Undo/Redo/selection/drag/resize/viewport/control-state mutation.
- Review traffic under concurrent Workflow/Monitor/Tag load and write guards on isolated equipment.
- Hardware-specific codecs/read widths, prolonged mixed-load/slow-device operation and soak remain
  pending. No production-control/security/24-hour reliability acceptance is implied.

## Known limitations and exclusions

Conservative grouping deduplicates only identical compatible configured ranges: no expanded adjacent
block reads without declared Device capabilities. Cross-owner duplicate reads remain possible.
No String Modbus codec, WORKFLOW_VARIABLE producer or Variable Blocks. No new auto-connect policy.
Higher-priority work can delay Tags beyond freshness thresholds. Values remain memory-only.
O2-B2 REST snapshot/Tag WS/replay and O2-B3 Overview live rendering are not implemented.
No MQTT/Sparkplug, Historian, HA, controls, assets, later phases, permissions or dependency upgrades.
The existing unauthenticated trusted-LAN boundary remains.

## Protected infrastructure changed

- `server/src/modbus.ts`: permanent bounded stream parser integration, correlation/session guards,
  retired cancelled/expired transaction IDs, read-only lowest-priority acquisition admission/cancellation.
- `server/src/index.ts`: service/config-route construction, Device/config lifecycle invalidation,
  application version literals. Existing WS handlers, Workflow/Monitor ownership and output code
  are unchanged apart from hello version metadata.
- New `server/src/modbusFraming.ts` encapsulates the bounded parser.

No edits to `runtimeSessions.ts`, `monitor.ts`, codecs, engine, write guards, Definition Catalog/routes,
resolver/reference code, Overview runtime rendering/persistence, App WS handling or liveTransport.
The only existing feature UI changed is the optional Shared Tag Acquisition action in Data Sources;
`overviewState.test.ts` changes are version assertions only.

## Delivery status

- Full Gates: NOT RUN; requires Owner authorization after intermediate review.
- Commit: **NOT CREATED**.
- Push: **NOT PERFORMED**.
- PR/Tag/Release/ZIP: not created.

## Exact changed files (31: 14 modified, 17 new)

### Modified

- `CHANGELOG.md`
- `README.md`
- `client/package.json`
- `client/src/components/sources/DataSourcesPage.tsx`
- `client/src/lib/overviewState.test.ts`
- `client/src/version.test.ts`
- `client/src/version.ts`
- `docs/CURRENT_STATE.md`
- `docs/ROADMAP.md`
- `package-lock.json`
- `package.json`
- `server/package.json`
- `server/src/index.ts`
- `server/src/modbus.ts`

### New

- `client/src/components/sources/AcquisitionMappingEditor.test.tsx`
- `client/src/components/sources/AcquisitionMappingEditor.tsx`
- `client/src/lib/acquisitionApi.ts`
- `docs/ACCEPTANCE_TESTS/O2-B1-v1.4.0-dev.6.md`
- `docs/SCOPE_O2-B1_v1.4.0-dev.6.md`
- `server/src/acquisitionConfig.ts`
- `server/src/acquisitionRoutes.ts`
- `server/src/modbusFraming.ts`
- `server/src/sharedTagAcquisition.ts`
- `server/src/tagRuntime.ts`
- `server/test/acquisitionConfig.test.ts`
- `server/test/acquisitionFixtures.ts`
- `server/test/acquisitionLifecycle.test.ts`
- `server/test/acquisitionRegression.test.ts`
- `server/test/modbusFraming.test.ts`
- `server/test/sharedTagAcquisition.test.ts`
- `server/test/tagRuntime.test.ts`


## Final read-only source verification

- 31 changed paths (14 modified, 17 new), exactly listed above; staged 0.
- All 180 other approved-base files are raw-byte identical to both pre-edit SHA-256 evidence and
  the approved `git archive`. A first comparison against raw Git blobs flagged the existing CRLF
  PowerShell script; `.gitattributes` specifies CRLF. Its original bytes were unchanged. The final
  comparison correctly uses the approved archive and pre-edit bytes, not LF-normalized blobs.
- Manifests/lock parsed comparison confirms only four application lock metadata versions and the
  three application manifest versions changed; dependencies, resolution and integrity unchanged.
- Final branch/HEAD/actual Remote remain the approved base; no commit or push.
- `git diff --check` PASS. No Full Gates, publish/hygiene suite or remote delivery performed.
