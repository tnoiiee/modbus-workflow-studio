# O2-B1 dev.7 punchlist acceptance and evidence

Base `55335b00ff7cf20c3fee14a09ebd46e52fb476d1` / dev.6.
Owner O2-B1 APPROVED WITH PUNCHLIST; Manual Disconnect PASS retained.
New Owner Local Manual Review **PENDING**. No browser/hardware/soak PASS inferred from tests.

## Final authorized fixture correction — validation complete

The latest Owner authorization supersedes the historical STOP states recorded below.
The one fixture-only correction serializes JSON using a value assembled from separate
non-sensitive fragments at test runtime, with an explanatory source comment. Generated JSON is
semantically identical to the original positive case and remains detected as assigned-secret.
The contextual scanner itself and every application source file were unchanged in this attempt.
Synthetic generated JSON is scanned in memory; CLI fixtures live outside tracked source in a
temporary directory and are cleaned up in finally. No new allowlist, suppression, exclusion or bypass.

| Final gate | Actual result |
| --- | --- |
| `node --test scripts/test/hygiene-check.test.mjs` (one authorized retry) | **56/56 PASS**, 0 skipped |
| Ordinary-code fixtures | **16 PASS**; exact 14 legacy findings reproduced and all three complete approved files now clean |
| Positive credential fixtures | **15 literal + 9 config + 11 embedded-rule tests PASS** |
| Other scanner checks | Placeholder behavior, deterministic output/path/line/bounds, strict CLI blocking and test-source self-scan PASS |
| `node scripts/hygiene-check.mjs --all --strict` (available retry) | **PASS: 445 files, 0 errors, 0 warnings** |
| `npm run verify:publish` | **PASS: 445 files, 0 errors, 0 warnings** |
| `git diff --check` | **PASS** |
| Final complete-worktree strict scan (includes new files) | **PASS: 226 files, 0 errors, 0 warnings** |
| Previously completed Full Client / Full Server | **605/41 / 250/17 PASS**, not rerun |
| Previously completed `npm run check` | **PASS**, not rerun |

The explicit all/history gates above ran before staging, so their tracked-tree count excludes new
untracked files; final publication review additionally scans the complete worktree and staged diff.
No accepted Acquisition/Traffic/runtime/application suite was rerun for this fixture-only change.
The 31-file dev.7 set remains within approved scope. Server production changes only index version
literals; producer/REST/WS/queue/coalescing unchanged. Dependency versions/resolution/integrity
unchanged. No later-phase runtime delivery, diagnostics, controls or Variable producer added.

Fresh sandbox recovery for this attempt compared 226 preserved working files against the 211-file
approved Remote archive and found exactly the authorized 31 dev.7 paths, with no missing source.
Compare-and-swap update-ref and read-tree WITHOUT -u preserved every source checksum. Missing
dependencies were restored once using the approved command; source/manifests/lock hashes unchanged.
Temporary recovery evidence and ignored logs are not durable backups. This document records results.
One commit and normal development-branch push follow final scope verification; commit identity is
available from Git history and the final Owner handoff, not self-embedded in the commit's contents.
Owner Local Manual Review remains **PENDING**. No PR/Tag/Release/ZIP or later phase started.

## Historical attempts (superseded by final results above)

### Initial automated gate execution

Executed 2026-09-26; delivery STOPPED at strict hygiene (see below). Atomic order: Stage 1 targeted tests, Client then
Server typechecks/builds; Full Client, Full Server, `npm run check`, strict hygiene,
`npm run verify:publish`, `git diff --check`. One targeted correction and retry per failed command;
a failed retry stops delivery. No dependency reinstall/upgrade during gates.

### Executed results — blocked, no checkpoint commit/push

| Gate | Actual result |
| --- | --- |
| Stage 1 Acquisition (`acquisitionValidation`, `AcquisitionValidationUX`, existing editor tests) | PASS 59 tests / 3 files |
| Stage 1 Traffic (model, SSR and event-harness tests) | PASS 29 / 3 |
| Stage 1 runtime/config/framing/regression (`tagRuntime`, `sharedTagAcquisition`, `acquisitionRestart`, `acquisitionLifecycle`, `acquisitionConfig`, `acquisitionRegression`, `modbusFraming`) | PASS 140 / 7 |
| Stage 1 protected Client (all remaining test files, excluding the six Acquisition/Traffic files above) | PASS 517 / 35 |
| Stage 1 protected Server (all remaining top-level test files, excluding the seven above) | PASS 110 / 10 |
| Stage 1 Client typecheck → Server typecheck → Client build → Server build | All PASS, sequential |
| `npm test -w client -- --run` | PASS 605 / 41 |
| `npm test -w server -- --run` | PASS 250 / 17 |
| `npm run check` | PASS, both typechecks/tests/builds |
| `node scripts/hygiene-check.mjs --all --strict` | FAIL: 445 scanned files, 14 tree errors, 0 history errors, 0 warnings |
| `npm run verify:publish` | NOT RUN — stopped at preceding failed gate |
| `git diff --check` | NOT RUN — stopped at preceding failed gate |

No test/typecheck/build correction or retry was needed. The strict hygiene command has been run
once and failed; its one permitted targeted correction/retry has NOT been used. Delivery is
blocked pending Owner approval of the out-of-scope scanner correction. No bypass or suppression.

All 14 findings are the existing generic `assigned-secret` pattern treating non-credential runtime
`token` type annotations/expressions as secrets: `sharedTagAcquisition.ts` (4), `tagRuntime.ts` (5),
`tagRuntime.test.ts` (5). These THREE files AND `scripts/hygiene-check.mjs` are byte-identical to
approved base `55335b00ff7cf20c3fee14a09ebd46e52fb476d1`. This is current executed evidence;
historical dev.6 PASS reporting is not a substitute and does not override this failure.

A correction needs scanner scope approval; editing production runtime sources, inserting broad
allow markers, weakening the gate or claiming PASS is not authorized. Suggested next approval:
precise scanner false-positive correction plus regression tests retaining credential detection,
without production Server edits or dependency changes. Stop before making that correction.

Warnings: 66 existing React SSR `useLayoutEffect` warnings in Full Client (also in combined check).
Client JS bundle 657.32 kB / gzip 192.52 kB; Vite >500 kB warning retained, not suppressed.
Lockfile comparison: only root/client/server application versions changed; dependency entries
otherwise identical. Production Server diff: `index.ts` version values only, verified against base.
Actual Remote and local HEAD remain `55335b00ff7cf20c3fee14a09ebd46e52fb476d1`; no new commit,
normal push, PR, Tag, Release or ZIP. Uncommitted implementation is preserved on the session branch.
Local gate logs are ignored under `.hygiene-out/dev7/`; this document is the durable result summary,
not a claim that ignored logs or `/tmp` recovery evidence are durable backups.

### First authorized hygiene correction attempt — STOPPED on focused-test failure

Owner subsequently authorized narrow scanner-only correction/tests, with a mandatory STOP if
focused tests fail. Target remains dev.7; no application source or version edits in this attempt.
Sandbox recreation was detected. Fresh working hashes preserved 225 files; approved Remote archive
contained 211 files and the exact 29 expected dev.7 differences, with no missing/unexpected paths.
Recovered the session branch by compare-and-swap update-ref and read-tree WITHOUT -u; all working
file hashes unchanged. Restored dependencies once in the new environment using the authorized
command; application/lock/source hashes unchanged. This evidence is current-environment evidence,
not a durable backup claim. Actual Remote was verified as the approved dev.6 base before recovery.

Scanner change under test: distinguish unquoted JS/TS references/types/calls from literal values;
re-examine typed initializers and later same-line assignments; keep independent embedded-secret
rules and strict severity policy. No new file/directory/word allowlist or bypass. Node-only focused
tests require no dependency changes and construct synthetic credentials in memory/temp repositories.

`node --test scripts/test/hygiene-check.test.mjs`: **55 PASS / 1 FAIL (56 tests)**.
- All 16 ordinary-code fixture tests PASS.
- Exact 14 reported legacy findings reproduced; new scan of all three approved files clean: PASS.
- All 15 literal-assignment tests, 9 config tests and 11 embedded-rule tests PASS.
- Existing-placeholder, deterministic/path/line/bounds and strict CLI blocking tests PASS.
- FAIL: self-scan of the regression test source detects its intentionally positive JSON fixture
  at `scripts/test/hygiene-check.test.mjs:87`, rule `assigned-secret`, severity `warn` (strict would
  promote to error). The literal type-looking JSON credential value is correctly suspicious to
  the scanner; this fixture was stored directly instead of fully generated at test runtime.
  Masked evidence: `Acq############… (16 chars)`. This is synthetic fixture text, not a real secret.

Per Owner instruction, STOPPED immediately: no fixture correction, no test retry, no strict hygiene
retry, no verify:publish, no diff-check, no staging/commit/push. The remaining narrow next action is
fixture construction cleanup, NOT a detection exception; it has not been performed or authorized
for retry after this failed focused run. Prior Full Client 605/41, Server 250/17 and check PASS were
not rerun. Original strict hygiene failure remains historical; its retry has not yet been executed.
Working changes now include scanner and focused tests (31 changed files total). Remote checkpoint
remains dev.6, not dev.7; Owner manual review remains PENDING.

## Evidence coverage

- `acquisitionValidation.test.ts`: Server-aligned enums/bounds, zero/false, empty/partial/nonfinite,
  compatible FC/codec/Width, Boolean scale/offset, stale values and first-invalid focus helper.
- `AcquisitionValidationUX.test.tsx`: React hook/event harness for inline message association,
  clearing on correction, blocked invalid PUT, focus-ref callback, valid numeric payload, native
  stale options, explicit Width repair, impact notices and form-level Server error summary.
- Existing editor/config/catalog/Overview regression tests remain protected.
- `traffic.test.ts`, `TrafficMonitor.test.tsx`, `TrafficMonitorUX.test.tsx`: both observed shapes,
  fixed headers, proven origins/phase, missing/malformed/zero/false, bounded escaped details,
  no dedup/grouping, occurrence keys across prepends, 5000 retention and 50-row pagination.
- `sharedTagAcquisition.test.ts`: SHA256 AND exact bytes of Definitions, Page, Page index,
  mapping and independent Control-state, unchanged revision/modifiedAt through reload;
  actual Draft/history helper invariants; delayed success/error fenced after Disconnect.
- `acquisitionRestart.test.ts`: two real child processes on same persisted directory, production
  Store/config/service classes, fake Device connection, private test IPC only. First GOOD/42;
  next fresh epoch, initial UNCERTAIN/NO_SAMPLE, then DISCONNECTED/no sample/no last-good,
  no reads or connects; mappings and protected files unchanged. Not actual Server observation.
- `acquisitionLifecycle.test.ts`: actual application Server/WS/TCP simulator and process restart
  on same DATA_DIR; config retained, Browser reconnect and restart never connect/read, explicit
  connect starts acquisition, Browser close does not stop it, manual disconnect stops it;
  no Tag WS and no runtime snapshot endpoint. No new production diagnostic surface.

## Owner-local Acquisition and Traffic checklist — PENDING

Use a safe local simulator only, `ALLOW_WRITES=false`, isolated DATA_DIR; stop unrelated owners.
Do not aim validation at hardware or run real writes. Keep current Owner Disconnect PASS separate.

1. Open Data Sources → Shared Tag → Acquisition. Try Address -1: immediate below-field
   **Address must be zero or greater.** Save must make no PUT and focus that input. Change to 0:
   error clears immediately. Try empty, '-', '1e', NaN, Infinity, fractional/out-of-bounds integers;
   incomplete drafts remain visible, never silently 0. Review scale/offset and threshold limits.
2. Tab through every field/combobox, activate invalid Save with keyboard, check associated readable
   error text with screenreader and non-color cues. Server validation errors remain form-level.
   Browser/screenreader behavior is NOT proven by the React event harness.
3. Test Number/Boolean/disabled/String Definitions and available/disabled/missing Devices. Use only
   supported native choices; stale invalid mapping options stay explicit, no substitute defaults.
   Definition-incompatible choices show why disabled. FC change preserves other values. Codec
   changes only derived Width with notice; Scale/Offset remain. Verify correction path for stale
   Width using mocked local response/test fixture, never by corrupting operational config files.
4. With simulator connected, view Workflow, Monitor, acquisition and generic traffic. Columns must
   not change when a new origin arrives; result/payload remain visible. Phase only TX/RX/ERROR;
   no claim default requestClass proves Workflow. Check context IDs and error details.
5. Long payload/error, partial records and hostile markup in local test fixtures must not execute,
   stretch primary rows or crash. Keyboard toggle details/pagination/horizontal scroll at narrow
   and desktop widths. Scroll details for full available bounded text/truncation notice.
   Over 50 records uses pages; no dedup/grouping, view preserves received order. WS is best-effort.

## Owner-local persistence boundary checklist — PENDING

1. First create/save the Shared Tag mapping and Overview Page, seed one independent Preview
   Control-state, and let startup configuration migrations settle. Record the Page revision and
   SHA256/bytes of `source-definitions.json`, `shared-tag-acquisition.json`, `overview-pages.json`,
   `overview-pages/<id>.json`, `overview-control-states.json` in the isolated DATA_DIR. Keep this
   evidence outside the repository and never include credentials or operational data in a commit.
2. Establish an unsaved Overview draft edit and known Undo/Redo state, then stop changing it.
   Save/Cancel and Preview controls must NOT be clicked during observation; they legitimately
   persist configuration/state and would invalidate the baseline. Observe Page revision and
   unsaved marker/history availability; check that acquisition alone adds no history actions.
3. Explicitly connect the existing Device once. Acquire from the simulator, close/reopen Browser,
   disconnect manually and wait beyond poll/stale thresholds. Recompare all five file hashes,
   revision and draft/history. No reconnect may be triggered by Browser or enabled mapping.
   There are no Browser Tag values: do NOT use Traffic RX as proof of canonical Tag GOOD.
4. Stop/restart Server with SAME isolated DATA_DIR. Mapping should load unchanged; no Device
   auto-connect, no polling until explicit Device connect, no Tag WS. Recompare files/revision
   after startup settles. Unsaved Browser draft is not a persisted runtime value and need not
   survive a full Browser reload. Its in-session no-runtime-mutation is a separate check.
5. Invisible Store quality/epoch/last-good and controlled stale completion checks are supplied by
   the automated production-class harness/tests above, NOT a console observer or new endpoint.
   Run them locally if desired; label that harness evidence, not manual in-process observation.
   A Store created independently in a console is not the running service's Store.
6. Mark manual outcomes individually. Browser accessibility/layout, actual hardware timing and
   extended/soak remain untested until Owner records results. O2-B2/B3/C/D are NOT STARTED.

## Known limitations and dependencies

No behavior changes to production Server, retention, WS coalescing or duplicate physical reads
across owners. View-local keys are not persisted/global producer identity. No complete capture.
Existing SSR useLayoutEffect and bundle-size warnings must be reported, not suppressed.
Advisories **5 moderate / 1 high / 1 critical**: not resolved, not accepted, outside this punchlist.
