# Acceptance Test v1.2.10

Status: published hotfix scope; command evidence below was run in this checkout on `2026-09-20` UTC.

## Scope

- Google Sans local font assets and the v1.2.10 CSS specificity/font-loading hotfix remain part of the published application scope.
- The hygiene-toolkit and documentation work in this change does not alter React Flow, workflow runtime, Modbus semantics or safety, monitor scheduling, WebSocket behavior, or output behavior.
- The v1.2.11 Monitor Scheduler & WebSocket Reliability work remains a proposed, unapproved item.

## Evidence actually run

| Command | Result and evidence |
| --- | --- |
| `npm ci --include=optional` | PASS. Installed 249 packages and audited 252 packages. npm reported 7 existing audit findings (5 moderate, 1 high, 1 critical); no `npm audit fix` was run. |
| `npm run check` | PASS. Server typecheck passed; client typecheck passed; server tests passed: 4 tests; client tests passed: 1 test; server build passed; client production Vite build passed. |
| `node scripts/hygiene-check.mjs --all --strict` | PASS. `hygiene-check [tree+history] PASS` with 157 files, 0 errors, 0 warnings; tree 0 errors and history 0 errors. |
| `node scripts/build-toolkit-zip.mjs` | PASS. Created ignored `release/hygiene-toolkit.zip`, 17 payload files, SHA-256 `07717eb1b121cc4188d4b2e1d17088b37154ad82c5b40fde8fe668e035b3f515`. |
| `node scripts/verify-toolkit-zip.mjs` | PASS: `Checks: 14/14 passed`. Archive integrity, payload contents, manifest hashes, `.mjs` syntax, shell syntax, hook executable bits, workflow structure, and payload hygiene scan passed. The environment did not have a YAML parser, so the verifier used its documented structural YAML fallback. |

The build outputs, client/server `dist` directories, TypeScript build metadata, `node_modules`, ZIP and hygiene report artifacts are ignored and were not added to Git.

## Not run in this acceptance record

- Browser DevTools font rendering checks, `document.fonts.check`, computed/rendered-font inspection, and network status checks were not run in this environment.
- Live Modbus TCP hardware/device checks, long-running monitor load tests, WebSocket reconnect tests, and end-to-end browser workflow tests were not run.
- GitHub Actions CI, a post-push fresh-clone verification, GitHub secret-scanning/push-protection behavior, and repository visibility changes were not run here.

Do not treat the unrun checks as passed. Run them in the owner-controlled release environment before adding or changing a release tag.
