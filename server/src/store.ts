import fs from'node:fs';import path from'node:path';export class JsonStore<T>{constructor(private file:string,private fallback:T){fs.mkdirSync(path.dirname(file),{recursive:true})}load():T{try{return JSON.parse(fs.readFileSync(this.file,'utf8'))as T}catch{return structuredClone(this.fallback)}}save(data:T){const tmp=this.file+'.tmp';fs.writeFileSync(tmp,JSON.stringify(data,null,2));fs.renameSync(tmp,this.file)}}
